# Makes bare `pytest` work the same as `python -m pytest`:
# without `pythonpath`, conftest.py's `import app.models` fails with
# ModuleNotFoundError (CI runs bare pytest; python -m adds the cwd to
# sys.path, which is why local runs never saw this).
[pytest]
pythonpath = .
testpaths = tests
